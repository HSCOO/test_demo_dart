import 'package:flutter/material.dart';

class DangAnPage extends StatelessWidget{
  const DangAnPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("About"),
    );
  }
}